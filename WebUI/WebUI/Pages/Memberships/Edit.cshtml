@page
@model WebUI.Pages.Memberships.EditModel
@{
    ViewData["Title"] = "Editar Membresía";
}

<div class="container mt-4">
    <h1>@ViewData["Title"]</h1>
    <hr />

    @if (TempData["SuccessMessage"] != null)
    {
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    }

    <form method="post">
        <!-- Esto asegura que el Id se envíe correctamente -->
        <input type="hidden" asp-for="Membership.Id" />

        <div class="mb-3">
            <label asp-for="Membership.Name" class="form-label"></label>
            <input asp-for="Membership.Name" class="form-control" />
        </div>

        <div class="mb-3">
            <label asp-for="Membership.Price" class="form-label"></label>
            <input asp-for="Membership.Price" class="form-control" type="number" step="0.01" />
        </div>

        <div class="mb-3">
            <label asp-for="Membership.Description" class="form-label"></label>
            <input asp-for="Membership.Description" class="form-control" />
        </div>

        <div class="mb-3">
            <label asp-for="Membership.MonthlySessions" class="form-label"></label>
            <input asp-for="Membership.MonthlySessions" class="form-control" type="number" />
        </div>

        <div class="form-check mb-3">
            <input asp-for="Membership.IsActive" class="form-check-input" />
            <label asp-for="Membership.IsActive" class="form-check-label"></label>
        </div>

        <button type="submit" class="btn btn-primary">Guardar</button>
        <a asp-page="Index" class="btn btn-secondary">Cancelar</a>
    </form>
</div>